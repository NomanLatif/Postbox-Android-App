<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    <CheckBoxPreference
        android:key="example_checkbox"
        android:title="@string/pref_title_social_recommendations"
        android:summary="@string/pref_description_social_recommendations"
        android:defaultValue="true" />-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <!--<EditTextPreference
        android:key="@string/pref_key_min_distance"
        android:title="@string/pref_title_min_update_distance"
        android:defaultValue="@integer/pref_default_min_update_distance"
        android:selectAllOnFocus="true"
        android:inputType="number"
        android:singleLine="true"
        android:numeric="integer"
        android:maxLines="1" />-->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <ListPreference
        android:key="@string/pref_key_min_distance"
        android:title="@string/pref_title_min_update_distance"
        android:defaultValue="10"
        android:entries="@array/pref_min_distance_update_list_titles"
        android:entryValues="@array/pref_min_distance_update_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
